================================================================================
DEEPAXON v4 — TO-DO LIST v2
Updated: 2026-03-25
Previous session: Full codebase audit (Session 1)
This session: Bug fixing, structural refactoring, documentation (Session 2)
================================================================================

Priority legend:
  🔴  Critical   — bug or verification required before results can be trusted
  🟠  High       — significant issue or planned breaking change
  🟡  Medium     — improvement, cleanup, or minor bug
  📄  Docs       — documentation or commenting task
  ✅  Resolved   — completed this session or previous session

================================================================================
SECTION 1 — CRITICAL (remaining)
================================================================================

[CRIT-02] BGW Color Mapping — Verification Required
  File:    morphometrics/morphometrics.py  →  get_morphometrics()
  Status:  Docstrings and comments corrected. axon_mask = cv2.inRange(img, 200, 255)
           confirmed correct (white=axon). Needs real-world verification pass
           against actual segmented images to confirm results are anatomically correct.
  Action:  Run morphometrics on a known sample, visually confirm axon count
           and g-ratio values are physiologically plausible.

[CRIT-08] ROI Coordinate Space — Verification Required
  File:    batch_axon/overlay/export_roi.ijm
  File:    batch_axon/overlay/process_overlay.py
  Status:  Not yet verified.
  Action:  Confirm what resolution CSA overlay .tif files are saved at by Fiji.
           Ensure image opened in get_overlay_area() matches the coordinate
           space the macro exports. Add comment documenting confirmed assumption.

================================================================================
SECTION 2 — HIGH PRIORITY (remaining)
================================================================================

[HIGH-01] Python 3.14/3.15 Migration
  Status:  Not started — planned for future.
  Action:  Full dependency audit required when migration begins. TF 2.10.1 +
           Keras 2.10.0 will need replacing. All unpinned packages need
           re-testing. tf.config.experimental.set_memory_growth and
           tensorflow.keras.backend (K) usage in metrics.py need verification.

================================================================================
SECTION 3 — RESOLVED THIS SESSION
================================================================================

✅ CRIT-01  Normalization — verified L2 (axis=1) correct for existing models.
            data_loader.py updated to normalize(arr, axis=1).
            segment.py explicit axis=1 added with cross-reference comment.

✅ CRIT-03  prepare_dataset() NameError — cropped_img/cropped_mask defined.

✅ CRIT-04  mag=model_type bug — mag parameter added throughout train pipeline.

✅ CRIT-05  Duplicate axon assignment — assigned_axon_labels set added to
            morphometrics matching loop.

✅ CRIT-06  Mask resize interpolation — INTER_NEAREST for masks, INTER_LANCZOS4
            for images. Matches v1 pipeline.

✅ CRIT-07  Mask encoding — base_label() + to_categorical() added to training
            pipeline. Matches v1 pipeline.

✅ HIGH-02  100X CSA workflow — get_image_csa() 100X branch returns None.
            Axon density for 100X uses fourx_csa only.

✅ BUG-01   get_fiji_path() cache staleness — _config_cache updated after save.
✅ BUG-02   compute_batch_size() fallback — warnings.warn() added.
✅ BUG-03   table() removed — dead code, never called.
✅ BUG-04   logging/timing defaults — False across all entry points.
✅ BUG-05   morphometrics log path — uses study_dir not input_dir.
✅ BUG-06   image/mask pair validation — added to batch_process().
✅ BUG-07   silent patch load warning — print added on failed load.
✅ BUG-08   val directory check — filters to image extensions only.
✅ BUG-09   workbook try/finally — workbook.close() guaranteed.
✅ BUG-10   axon_diam_px → axon_diam column name fixed.

✅ IMP-01   import re module-level in console.py
✅ IMP-03   PIL import module-level in resize.py
✅ IMP-04   list_files module-level in preprocess.py
✅ IMP-05   import cv2 module-level in augment.py
✅ IMP-06   unused import os removed from data_loader.py
✅ IMP-07   deferred import comment added in unet_plus_plus.py
✅ IMP-08   inline rich imports moved to module-level in train.py
✅ IMP-09   get_image_resolution module-level in morphometrics __main__.py
✅ IMP-10   peak_local_max removed from morphometrics.py

✅ STR-01   center_crop() extracted to utils/helpers.py
✅ STR-04   _has_images() extracted to module-level in helpers.py
✅ STR-08   TARGET_SIZE imported from resize.py in analyze_nerve.py
✅ STR-12   Root directory cleanup — notebooks/ and sample_images/ created

✅ NAME-01  train/models/ → train/architectures/
✅ NAME-02  train/data/ → train/dataset/
✅ NAME-03  UNET() → build_unet()
✅ NAME-04  UNET_PLUS_PLUS() → build_unet_plus_plus()
✅ NAME-05  fibre → fiber standardized in morphometrics.py
✅ STR-03   build_model() factory moved to train/architectures/__init__.py

✅ FEAT-01  GPU non-interactive — batch_axon doesn't call GPU setup
✅ FEAT-02  Retry input loop — all three entry points
✅ FEAT-03  Auto-promote model — done in train.py
✅ FEAT-05  aug_prob from actual patch count — recalculated in train.py
✅ FEAT-06  Resolution mismatch warn-only — segment.py
✅ FEAT-08  Augmentation params to config — augment.py reads from config
✅ FEAT-09  Watershed params to config — morphometrics.py reads from config
✅ FEAT-11  Configurable segmented suffix — config-driven in segment.py and morphometrics
✅ FEAT-12  Validate image_rows keys — .get() with defaults in write_nerve_block()
✅ FEAT-13  Cache xlsx reads — per_file dict in analyze_nerve.py
✅ FEAT-20  Callback constants to config — training block in config.json

✅ DOC-01   Hann window comment added in segment.py
✅ DOC-02   BGW contract comment in recolor()
✅ DOC-03   Normalization cross-reference comments in segment.py and data_loader.py
✅ DOC-04   Dead code removed from segment.py
✅ DOC-05   Dead commented-out block removed from export_roi.ijm
✅ DOC-06   Watershed threshold comments added in get_labels()
✅ DOC-08   myelin_thickness formula comment added
✅ DOC-09   TF pin rationale in README and requirements.txt
✅ DOC-11   train.py flow comment in train_model() docstring
✅ DOC-12   smooth=1.0 comment in metrics.py
✅ DOC-13   filters=16 rationale in unet_plus_plus.py
✅ DOC-14   README major additions complete
✅ GEN-03   Log file location — morphometrics uses study_dir consistently

================================================================================
SECTION 4 — REMAINING MEDIUM PRIORITY
================================================================================

[STR-02] Extract conv_block() to train/architectures/blocks.py
  File:    train/architectures/unet.py
  File:    train/architectures/unet_plus_plus.py
  Issue:   conv_block() is identically defined in both architecture files.
  Action:  Create train/architectures/blocks.py containing conv_block().
           Import from there in both unet.py and unet_plus_plus.py.

[STR-05] Extract select_model() to utils/helpers.py
  File:    segment/__main__.py  →  select_model()
  Issue:   Generic interactive selection pattern reusable elsewhere.
  Action:  Move to utils/helpers.py as select_from_list(items, prompt, display_fn).
           Update segment/__main__.py to call from utils.

[STR-06] Extract Excel helpers to batch_axon/excel_utils.py
  File:    batch_axon/__main__.py  →  add_formats(), write_nerve_block()
  Action:  Create batch_axon/excel_utils.py. Import in __main__.py.

[STR-09] Add evaluate/ module
  Issue:   No formal evaluation pipeline. No way to test a model against
           held-out data and report metrics.
  Action:  Create evaluate/ module with __main__.py that accepts a model path
           and test image/mask directory, runs inference, computes dice_np()
           and iou_np(), saves results to .xlsx or .csv.

[STR-10] Add models/model_registry.json
  Action:  Create deepaxon/models/model_registry.json with entry per .keras
           file: file, magnification, dataset, version, patch_size, trained
           date, val_dice, notes.

[STR-11] Archive rb_40x_v1_256.keras
  Action:  Move to deepaxon/models/archive/ — oldest model, superseded by v2.

================================================================================
SECTION 5 — REMAINING FEATURES
================================================================================

[FEAT-07] Configurable Loss Weights
  File:    utils/metrics.py  →  combined_loss()
  Action:  Add to config.json:
             "loss_weights": {"dice": 0.5, "bce": 0.5}
           Read via load_config() in combined_loss().

[FEAT-10] Configurable Patch Overlap
  File:    train/dataset/preprocess.py and segment/segment.py
  Action:  Add "overlap": 0.5 to config.json patch_size section.
           Compute step as int(patch_size * (1 - overlap)).

[FEAT-14] Pass patch_size to export_roi.ijm as Argument
  File:    batch_axon/overlay/process_overlay.py
  File:    batch_axon/overlay/export_roi.ijm
  Action:  Update command_args to include patch_size as third argument.
           Parse args[2] in macro. Currently hardcoded as 256.

[FEAT-15] Surface Fiji stderr on Failure
  File:    batch_axon/overlay/process_overlay.py  →  run_fiji_export()
  Action:  On CalledProcessError, add:
             stderr_msg = e.stderr.decode(errors='replace')
             print(f"[ERROR] Fiji stderr: {stderr_msg}")

[FEAT-16] Add Logger Parameter to process_overlay.py
  File:    batch_axon/overlay/process_overlay.py
  Action:  Add log: DeepAxonLogger = None parameter to run_fiji_export()
           and get_overlay_area(). Use log.warn()/log.error() when provided,
           fall back to print() otherwise.

[FEAT-17] ROI Coordinate Validation
  File:    batch_axon/overlay/process_overlay.py  →  get_overlay_area()
  Action:  After roi.coordinates(), add:
             coords = list(roi.coordinates())
             if len(coords) < 3:
                 print(f"[WARN] Degenerate ROI in {img_path.name}")
                 return None

[FEAT-18] Use tempfile for .roi Temp Files
  File:    batch_axon/overlay/process_overlay.py
  Action:  Replace roi_path = img_path.with_suffix('.roi') with
           tempfile.NamedTemporaryFile(suffix='.roi', delete=False).
           Avoids write permission issues and parallel run conflicts.

[FEAT-19] Per-Line Timestamps in Log File
  File:    utils/console.py
  Action:  Add optional timestamp_lines: bool = False to DeepAxonLogger.
           When True, prefix each _append() call with HH:MM:SS timestamp.

================================================================================
SECTION 6 — REMAINING DOCUMENTATION
================================================================================

[DOC-07] G-Ratio Option Labels
  File:    morphometrics/morphometrics.py
  Status:  Variables renamed to gratio_area and gratio_axes. Consider whether
           more descriptive names (gratio_equiv_diam, gratio_mean_axes) would
           be clearer. Low priority since current names are reasonable.

[DOC-10] Pin All Package Versions
  File:    requirements.txt
  Action:  Run pip freeze in current working environment and record exact
           versions for all unpinned packages: opencv-python, Pillow,
           patchify, scikit-image, scipy, numpy, pandas, openpyxl,
           xlsxwriter, roifile, rich, scikit-learn.

[DOC-15] ROI Type Documentation
  File:    README.md and batch_axon/overlay/process_overlay.py docstring
  Action:  Document that CSA overlays must be polygon ROIs in Fiji.
           Point, line, or freehand ROIs will produce incorrect results.

================================================================================
SECTION 7 — GENERAL
================================================================================

[GEN-01] 100X Pixel Size Calibration
  File:    config.json  →  pixel_size_um  →  "100X"
  Action:  Calibrate using stage micrometer at 100X. Set to {"1440": X.XXX}.
           Physical unit columns in morphometrics output will then populate.

[GEN-02] augment_dataset_np() Change Detection
  Status:  Resolved — np.array_equal removed, always replaces. ✅

[GEN-04] Multi-GPU Strategy
  File:    utils/gpu.py
  Action:  Document as future enhancement: add tf.distribute.MirroredStrategy
           option for multi-GPU training runs.

[GEN-05] Model Naming Convention
  File:    deepaxon/models/
  Action:  Apply {cohort}_{mag}_v{version}_{patch_size}_{YYYYMMDD}.keras
           convention to any newly trained models.

================================================================================
SECTION 8 — GENERAL / MIGRATION
================================================================================

[MIG-01] Python 3.14/3.15 Migration Checklist
  When migration begins, verify/replace:
  - tensorflow==2.10.1 and keras==2.10.0 — will not survive, replace stack
  - tf.config.experimental.set_memory_growth — verify new API path
  - tensorflow.keras.backend (K) in utils/metrics.py — verify compatibility
  - All currently unpinned packages — re-test against new environment
  - Consider PyTorch migration as alternative to TF 2.16+ / Keras 3

================================================================================
END OF TO-DO LIST
Resolved this session: ~50 items
Remaining: 23 items
  Critical (verify):     2  (CRIT-02, CRIT-08)
  High:                  1  (HIGH-01 migration)
  Structural:            5  (STR-02, STR-05, STR-06, STR-09, STR-10, STR-11)
  Features:              8  (FEAT-07, FEAT-10, FEAT-14 through FEAT-19)
  Documentation:         3  (DOC-07, DOC-10, DOC-15)
  General:               4  (GEN-01, GEN-04, GEN-05, MIG-01)
================================================================================
