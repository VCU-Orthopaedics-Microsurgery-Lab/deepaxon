# DeepAxon v5.1.0 — requirements
# Branch: v5_analysis | Python: 3.11.x
#
# ── Install order ─────────────────────────────────────────────────────────────
#   Step 1 — Everything except torch and patchify:
#     pip install -r requirements.txt
#
#   Step 2 — PyTorch CUDA wheel (must come after to override any drift):
#     pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 \
#         --index-url https://download.pytorch.org/whl/cu121
#
#   Step 3 — Patchify (--no-deps required, cannot be in this file):
#     pip install patchify==0.2.3 --no-deps
#
#   Step 4 — Verify:
#     python utils/version.py
#
# Note: sympy is intentionally excluded — torch 2.5.1 pins it to 1.13.1
# automatically. Including it causes unnecessary version churn.
# Note: narwhals is a transitive dep of scikit-learn>=1.5 — not listed directly.

# ── Core ML ───────────────────────────────────────────────────────────────────
segmentation_models_pytorch==0.5.0
monai==1.5.2
timm>=1.0
safetensors

# ── Scientific stack ──────────────────────────────────────────────────────────
numpy==2.2.4
scipy>=1.13
scikit-learn>=1.5
scikit-image>=0.24
pandas>=2.2

# ── Imaging ───────────────────────────────────────────────────────────────────
opencv-python>=4.10
pillow
tifffile
imageio
matplotlib

# ── Data / IO ─────────────────────────────────────────────────────────────────
openpyxl
xlsxwriter
PyYAML

# ── Pipeline utilities ────────────────────────────────────────────────────────
rich
tqdm
click
typer
shellingham
joblib
threadpoolctl

# ── Networking / HuggingFace ──────────────────────────────────────────────────
huggingface_hub
requests
certifi
filelock
fsspec

# ── General utilities ─────────────────────────────────────────────────────────
packaging
mpmath
networkx
six
python-dateutil
tzdata
lazy-loader
colorama

# ── NOT included — install separately ────────────────────────────────────────
# torch==2.5.1+cu121        → Step 2 above
# torchvision==0.20.1+cu121 → Step 2 above
# patchify==0.2.3           → Step 3 above (--no-deps)
# sympy                     → managed automatically by torch 2.5.1 (pins 1.13.1)